# Basic-Voice-Assistant
Clara – Python Voice Assistant. She can greet you, recognize your speech, and perform basic tasks like searching Wikipedia, opening Google, telling the time, or launching Spotify.

## Requirements

Install the required libraries:

```bash
pip install pyttsx3 SpeechRecognition wikipedia
pip install pipwin
pipwin install pyaudio
